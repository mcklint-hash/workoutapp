
const builtInExercises = [{"name": "Bänkpress", "muscle": "Bröst", "note": "Klassisk pressövning för bröst, axlar och triceps."}, {"name": "Lutande hantelpress", "muscle": "Bröst", "note": "Bra för övre bröst och stabilitet."}, {"name": "Chest flyes", "muscle": "Bröst", "note": "Isolationsövning med fokus på stretch."}, {"name": "Push-ups", "muscle": "Bröst", "note": "Enkel kroppsviktsövning som passar alla nivåer."}, {"name": "Dips", "muscle": "Bröst", "note": "Tung kroppsviktsövning som även tar triceps."}, {"name": "Cable cross", "muscle": "Bröst", "note": "Bra kontaktövning för bröstet."}, {"name": "Maskinpress", "muscle": "Bröst", "note": "Stabil pressövning för kontrollerad belastning."}, {"name": "Pec deck", "muscle": "Bröst", "note": "Isolerar bröstet med jämn belastning."}, {"name": "Marklyft", "muscle": "Rygg", "note": "Tung helkroppsövning med stort ryggfokus."}, {"name": "Skivstångsrodd", "muscle": "Rygg", "note": "Bygger tjocklek i ryggen."}, {"name": "Latsdrag", "muscle": "Rygg", "note": "Bra vertikal dragövning."}, {"name": "Sittande rodd", "muscle": "Rygg", "note": "Kontrollerad övning för mitten av ryggen."}, {"name": "Pull-ups", "muscle": "Rygg", "note": "Kroppsviktsövning för lats och övre rygg."}, {"name": "Face pulls", "muscle": "Rygg", "note": "Bra för övre rygg och skulderstabilitet."}, {"name": "Enarms hantelrodd", "muscle": "Rygg", "note": "Fin stretch och bra kontakt sida för sida."}, {"name": "Straight arm pulldown", "muscle": "Rygg", "note": "Isolerar latsen bra."}, {"name": "T-bar row", "muscle": "Rygg", "note": "Tung rodd för ryggtjocklek."}, {"name": "Chins", "muscle": "Rygg", "note": "Vertikal dragövning med mer bicepsfokus."}, {"name": "Knäböj", "muscle": "Ben", "note": "Basövning för lår, säte och bål."}, {"name": "Benpress", "muscle": "Ben", "note": "Bra komplement till knäböj."}, {"name": "Raka marklyft", "muscle": "Ben", "note": "Fokus på baksida lår och säte."}, {"name": "Utfall", "muscle": "Ben", "note": "Bygger balans och benstyrka."}, {"name": "Benspark", "muscle": "Ben", "note": "Isolerar framsida lår."}, {"name": "Lårcurl", "muscle": "Ben", "note": "Isolerar baksida lår."}, {"name": "Hip thrust", "muscle": "Ben", "note": "Mycket bra för säte."}, {"name": "Vadpress", "muscle": "Ben", "note": "För vader."}, {"name": "Bulgarian split squat", "muscle": "Ben", "note": "Tuff enbensövning för ben och säte."}, {"name": "Hack squat", "muscle": "Ben", "note": "Maskinövning med fokus på framsida lår."}, {"name": "Axelpress", "muscle": "Axlar", "note": "Basövning för främre och mellersta axeln."}, {"name": "Sidolyft", "muscle": "Axlar", "note": "Isolerar mellersta delen av axeln."}, {"name": "Omvänd pec deck", "muscle": "Axlar", "note": "Bra för baksida axel."}, {"name": "Arnold press", "muscle": "Axlar", "note": "Variation av press med lång rörelsebana."}, {"name": "Front raises", "muscle": "Axlar", "note": "Isolerar främre axeln."}, {"name": "Cable lateral raises", "muscle": "Axlar", "note": "Konstant spänning för sidodelten."}, {"name": "Shrugs", "muscle": "Axlar", "note": "Övning för traps."}, {"name": "Upright row", "muscle": "Axlar", "note": "Träffar axlar och traps."}, {"name": "Bicepscurl", "muscle": "Armar", "note": "Klassisk bicepsövning."}, {"name": "Hammercurl", "muscle": "Armar", "note": "Tränar biceps och underarm."}, {"name": "Preacher curl", "muscle": "Armar", "note": "Stabil curlövning med strikt teknik."}, {"name": "Incline dumbbell curl", "muscle": "Armar", "note": "Ger lång stretch i biceps."}, {"name": "Triceps pushdown", "muscle": "Armar", "note": "Stabil och enkel tricepsövning."}, {"name": "French press", "muscle": "Armar", "note": "Tyngre tricepsövning."}, {"name": "Overhead rope extension", "muscle": "Armar", "note": "Bra stretch för triceps."}, {"name": "Close grip bench press", "muscle": "Armar", "note": "Tung tricepsövning."}, {"name": "Cable crunch", "muscle": "Mage", "note": "Bra för belastad magträning."}, {"name": "Hängande benlyft", "muscle": "Mage", "note": "Utmanande övning för bål och höftböjare."}, {"name": "Plankan", "muscle": "Mage", "note": "Stabilitetsövning för hela bålen."}, {"name": "Ab wheel", "muscle": "Mage", "note": "Tung bålövning med lång rörelse."}, {"name": "Russian twists", "muscle": "Mage", "note": "Rotationsövning för mage."}, {"name": "Dead bug", "muscle": "Mage", "note": "Bra kontrollövning för bålen."}];
const builtInPrograms = [{"id": "4-day", "name": "4 dagar", "description": "Upper / Lower två varv per vecka.", "days": [{"name": "Dag 1 – Upper", "exercises": ["Bänkpress", "Latsdrag", "Axelpress", "Bicepscurl"]}, {"name": "Dag 2 – Lower", "exercises": ["Knäböj", "Benpress", "Lårcurl", "Vadpress"]}, {"name": "Dag 3 – Upper", "exercises": ["Lutande hantelpress", "Sittande rodd", "Sidolyft", "Triceps pushdown"]}, {"name": "Dag 4 – Lower", "exercises": ["Raka marklyft", "Utfall", "Benspark", "Cable crunch"]}]}, {"id": "5-day", "name": "5 dagar", "description": "Push / Pull / Legs + Upper + Arms.", "days": [{"name": "Dag 1 – Push", "exercises": ["Bänkpress", "Lutande hantelpress", "Axelpress", "Triceps pushdown"]}, {"name": "Dag 2 – Pull", "exercises": ["Marklyft", "Latsdrag", "Skivstångsrodd", "Hammercurl"]}, {"name": "Dag 3 – Legs", "exercises": ["Knäböj", "Benpress", "Lårcurl", "Vadpress"]}, {"name": "Dag 4 – Upper", "exercises": ["Dips", "Pull-ups", "Sidolyft", "Bicepscurl"]}, {"name": "Dag 5 – Arms/Core", "exercises": ["Preacher curl", "French press", "Overhead rope extension", "Ab wheel"]}]}, {"id": "6-day", "name": "6 dagar", "description": "Push / Pull / Legs två varv per vecka.", "days": [{"name": "Dag 1 – Push A", "exercises": ["Bänkpress", "Lutande hantelpress", "Sidolyft", "Triceps pushdown"]}, {"name": "Dag 2 – Pull A", "exercises": ["Marklyft", "Latsdrag", "Sittande rodd", "Bicepscurl"]}, {"name": "Dag 3 – Legs A", "exercises": ["Knäböj", "Benpress", "Lårcurl", "Vadpress"]}, {"name": "Dag 4 – Push B", "exercises": ["Dips", "Chest flyes", "Arnold press", "French press"]}, {"name": "Dag 5 – Pull B", "exercises": ["Pull-ups", "Enarms hantelrodd", "Face pulls", "Hammercurl"]}, {"name": "Dag 6 – Legs B", "exercises": ["Raka marklyft", "Utfall", "Hip thrust", "Cable crunch"]}]}];
const defaultState = {workouts:[],settings:{userName:"",defaultRound:"2.5"},selectedProgramId:null,currentProgramDayIndex:0,activeWorkout:null,customExercises:[],customPrograms:[],editingProgramId:null};
function cloneState(){return JSON.parse(JSON.stringify(defaultState));}
function getState(){const raw=localStorage.getItem("minTraningStateV61"); if(!raw) return cloneState(); try{return {...cloneState(), ...JSON.parse(raw)};}catch{return cloneState();}}
function setState(s){localStorage.setItem("minTraningStateV61", JSON.stringify(s));}
function getAllExercises(){const s=getState(); return [...builtInExercises,...(s.customExercises||[])];}
function getAllPrograms(){const s=getState(); return [...builtInPrograms,...(s.customPrograms||[])];}
function formatKg(v){const n=Number(v||0); const dec=n%1===0?0:1; return new Intl.NumberFormat("sv-SE",{minimumFractionDigits:dec,maximumFractionDigits:2}).format(n)+" kg";}
function formatDateTime(v){return new Intl.DateTimeFormat("sv-SE",{dateStyle:"medium",timeStyle:"short"}).format(new Date(v));}
function roundToStep(v,step){return Math.round(v/step)*step;}
function findProgramById(id){return getAllPrograms().find(p=>p.id===id)||null;}
function getCurrentProgramDay(s){const p=findProgramById(s.selectedProgramId); return p ? p.days[s.currentProgramDayIndex] : null;}
function getLatestExerciseLog(name){const s=getState(); for(const w of s.workouts){const f=w.exercises.find(e=>e.exercise===name); if(f) return f;} return null;}
function getRecommendationForExercise(name){
  const s=getState();
  const log=getLatestExerciseLog(name);
  if(!log) return null;

  const failure=log.sets[2];
  if(!failure||!failure.weight||!failure.reps) return null;

  const step=Number(s.settings.defaultRound||2.5);
  const lastWeight=Number(failure.weight);
  const lastReps=Number(failure.reps);

  // Progression för set 3:
  // 5–6 reps = behåll vikt
  // 7+ reps = öka 5%, men minst ett steg
  let set3 = lastWeight;
  let increaseApplied = 0;

  if(lastReps >= 7){
    const rawIncrease = lastWeight * 0.05;
    const increase = Math.max(rawIncrease, step);
    set3 = roundToStep(lastWeight + increase, step);
    increaseApplied = set3 - lastWeight;
  }

  // Set 1 och 2 räknas bakåt från nya set 3-vikten
  // Set 2 ≈ 10 reps
  // Set 1 ≈ 12 reps
  const set2 = Math.max(0, roundToStep(set3 * 0.90, step));
  const set1 = Math.max(0, roundToStep(set3 * 0.80, step));

  return { lastWeight, lastReps, set1, set2, set3, increaseApplied };
}
function switchView(id){document.querySelectorAll(".view").forEach(v=>v.classList.toggle("active",v.id===id)); document.querySelectorAll(".nav-btn").forEach(b=>b.classList.toggle("active",b.dataset.view===id));}
document.querySelectorAll(".nav-btn").forEach(b=>b.addEventListener("click",()=>switchView(b.dataset.view)));

function renderDashboard(){const s=getState(); const p=findProgramById(s.selectedProgramId); const day=getCurrentProgramDay(s); totalWorkouts.textContent=s.workouts.length; totalExercises.textContent=getAllExercises().length; activeProgramName.textContent=p?p.name:"-"; nextWorkoutHeadline.textContent=day?day.name:"Välj ett upplägg för att komma igång"; nextWorkoutSubtext.textContent=day?`Nästa övning: ${day.exercises[0]||"-"} • Upplägg: ${p.name}`:"När du valt ett upplägg visas nästa pass och första övningen här."; dashboardProgram.innerHTML=p?`<div class="mini-card"><strong>${p.name}</strong><div>${p.description||""}</div><div>${day?day.name:"-"}</div></div>`:"Inget upplägg valt ännu."; dashboardHistory.innerHTML=s.workouts.length?s.workouts.slice(0,3).map(w=>`<div class="mini-card"><strong>${w.dayName}</strong><div>${formatDateTime(w.createdAt)}</div><div>${w.exercises.length} övningar loggade</div></div>`).join(""):"Ingen historik ännu."; }

function renderPrograms(){const s=getState(); const arr=getAllPrograms(); programGrid.innerHTML=arr.map(p=>`<article class="program-card ${s.selectedProgramId===p.id?"active-program":""}"><h3>${p.name}</h3><p class="muted">${p.description||""}</p><div class="program-meta">${p.type==="custom"?"Eget 2-veckorsupplägg":"Förinställt upplägg"}</div><ul class="program-day-list">${p.days.map(d=>`<li>${d.name}</li>`).join("")}</ul><div class="button-row" style="margin-top:14px;"><button type="button" data-program="${p.id}">${s.selectedProgramId===p.id?"Valt upplägg":"Välj upplägg"}</button>${p.type==="custom"?`<button type="button" class="secondary" data-edit="${p.id}">Redigera</button>`:""}</div></article>`).join(""); programGrid.querySelectorAll("[data-program]").forEach(btn=>btn.onclick=()=>{const s=getState(); s.selectedProgramId=btn.dataset.program; s.currentProgramDayIndex=0; s.activeWorkout=null; setState(s); renderAll();}); programGrid.querySelectorAll("[data-edit]").forEach(btn=>btn.onclick=()=>startEditProgram(btn.dataset.edit)); }

function renderLibrary(){const search=librarySearch.value.trim().toLowerCase(); const filter=libraryFilter.value; const items=getAllExercises().filter(e=>e.name.toLowerCase().includes(search)&&(filter==="Alla"||e.muscle===filter)); exerciseLibrary.innerHTML=items.length?items.map(e=>`<article class="exercise-card"><span class="badge">${e.muscle}</span><h4>${e.name}</h4><p class="muted">${e.note||""}</p></article>`).join(""):'<div class="muted">Inga övningar matchade din sökning.</div>'; }
librarySearch.addEventListener("input", renderLibrary); libraryFilter.addEventListener("change", renderLibrary); resetLibrary.onclick=()=>{librarySearch.value=""; libraryFilter.value="Alla"; renderLibrary();};
addExerciseForm.addEventListener("submit", e=>{e.preventDefault(); const name=newExerciseName.value.trim(); if(!name)return; const s=getState(); const muscle=newExerciseMuscle.value; const note=newExerciseNote.value.trim(); if(getAllExercises().some(x=>x.name.toLowerCase()===name.toLowerCase())){addExerciseMessage.classList.remove("hidden"); addExerciseMessage.textContent="Övningen finns redan."; return;} s.customExercises.unshift({name,muscle,note}); setState(s); e.target.reset(); addExerciseMessage.classList.remove("hidden"); addExerciseMessage.textContent="Övningen lades till."; renderAll();});

function renderHistory(){historyList.innerHTML=getState().workouts.length?getState().workouts.map(w=>`<article class="history-item"><div class="history-top"><div><strong>${w.dayName}</strong><div>${w.programName}</div></div><div>${formatDateTime(w.createdAt)}</div></div><ul class="simple-list">${w.exercises.map(ex=>`<li><strong>${ex.exercise}</strong> – ${ex.sets.map((set,i)=>`set ${i+1}: ${set.weight||0} kg × ${set.reps||0}`).join(" • ")}${ex.failure?" • failure på set 3":""}</li>`).join("")}</ul></article>`).join(""):"Ingen historik ännu."; }

function updateWorkoutProgramStatus(){const s=getState(); const p=findProgramById(s.selectedProgramId); const day=getCurrentProgramDay(s); workoutProgramStatus.innerHTML=p?`Valt upplägg: <strong>${p.name}</strong>${day?` • Aktiv dag: <strong>${day.name}</strong>`:""}`:"Inget upplägg valt ännu."; }

function startWorkout(){const s=getState(); const p=findProgramById(s.selectedProgramId); const d=getCurrentProgramDay(s); if(!p||!d) return false; s.activeWorkout={programId:p.id,programName:p.name,dayName:d.name,currentExerciseIndex:0,currentSetIndex:0,exercises:d.exercises.map(name=>({exercise:name,sets:[{weight:"",reps:""},{weight:"",reps:""},{weight:"",reps:""}],failure:false,note:""}))}; setState(s); return true;}
function renderWorkout(){const s=getState(); updateWorkoutProgramStatus(); workoutComplete.classList.add("hidden"); if(!s.selectedProgramId){workoutNotReady.classList.remove("hidden"); workoutRunner.classList.add("hidden"); workoutNotReady.innerHTML='Välj först ett upplägg under fliken <strong>Upplägg</strong>, och tryck sedan på <strong>Starta dagens pass</strong>.'; return;} if(!s.activeWorkout){workoutNotReady.classList.remove("hidden"); workoutRunner.classList.add("hidden"); const d=getCurrentProgramDay(s); workoutNotReady.innerHTML=`Valt upplägg finns. Aktiv dag: <strong>${d?d.name:"-"}</strong>. Tryck på <strong>Starta dagens pass</strong>.`; return;} const w=s.activeWorkout; const entry=w.exercises[w.currentExerciseIndex]; if(!entry){workoutRunner.classList.add("hidden"); workoutNotReady.classList.add("hidden"); workoutComplete.classList.remove("hidden"); workoutComplete.innerHTML=`<strong>Passet är klart.</strong><br>${w.dayName} sparades i historiken.`; return;} workoutNotReady.classList.add("hidden"); workoutRunner.classList.remove("hidden"); currentExerciseName.textContent=entry.exercise; currentExerciseMeta.textContent=`${w.dayName} • 3 set per övning`; exerciseProgress.textContent=`${w.currentExerciseIndex+1} / ${w.exercises.length}`; const human=w.currentSetIndex+1; currentSetTitle.textContent=`Set ${human} av 3`; setStepPill.textContent=`Set ${human}`; failureWrap.classList.toggle("hidden", human!==3); const rec=getRecommendationForExercise(entry.exercise); if(!rec){exerciseRecommendation.innerHTML="Ingen tidigare loggning för den här övningen ännu.";} else {
  const progressionText = rec.increaseApplied > 0
    ? `<div><strong>Progression:</strong> +${formatKg(rec.increaseApplied)} på set 3 eftersom du klarade ${rec.lastReps} reps.</div>`
    : `<div><strong>Progression:</strong> behåll samma vikt på set 3.</div>`;
  exerciseRecommendation.innerHTML=`<div><strong>Senast:</strong> ${formatKg(rec.lastWeight)} × ${rec.lastReps} reps på failure-setet.</div>${progressionText}<div style="margin-top:8px;"><strong>Nästa gång:</strong></div><div>Set 1: <strong>${formatKg(rec.set1)}</strong> för <strong>12 reps</strong></div><div>Set 2: <strong>${formatKg(rec.set2)}</strong> för <strong>10 reps</strong></div><div>Set 3: <strong>${formatKg(rec.set3)}</strong> för <strong>5–7 reps</strong></div>`;
} setWeight.value=entry.sets[w.currentSetIndex].weight||""; setReps.value=entry.sets[w.currentSetIndex].reps||""; setFailure.checked=human===3; exerciseNote.value=entry.note||""; loggedSetsPreview.innerHTML=entry.sets.map((set,i)=>set.weight||set.reps?`<div>Set ${i+1}: ${set.weight||0} kg × ${set.reps||0} reps</div>`:"").filter(Boolean).join("") || "Inga set loggade ännu."; }
startWorkoutBtn.onclick=()=>{if(startWorkout()) renderAll();}; resetCurrentWorkoutBtn.onclick=()=>{const s=getState(); s.activeWorkout=null; setState(s); renderAll();};
fillRecommendedBtn.onclick=()=>{
  const s=getState();
  if(!s.activeWorkout) return;
  const entry=s.activeWorkout.exercises[s.activeWorkout.currentExerciseIndex];
  const rec=getRecommendationForExercise(entry.exercise);
  if(!rec) return;
  const idx=s.activeWorkout.currentSetIndex;
  if(idx===0){ setWeight.value=rec.set1; setReps.value=12; }
  if(idx===1){ setWeight.value=rec.set2; setReps.value=10; }
  if(idx===2){ setWeight.value=rec.set3; setReps.value=6; }
};
setLoggerForm.addEventListener("submit", e=>{e.preventDefault(); const s=getState(); const w=s.activeWorkout; if(!w) return; const entry=w.exercises[w.currentExerciseIndex]; const idx=w.currentSetIndex; const weight=Number(setWeight.value); const reps=Number(setReps.value); if(!weight||!reps) return; entry.sets[idx]={weight,reps}; entry.note=exerciseNote.value.trim(); if(idx===2){entry.failure=setFailure.checked; w.currentExerciseIndex += 1; w.currentSetIndex = 0; if(w.currentExerciseIndex>=w.exercises.length){s.workouts.unshift({programName:w.programName,dayName:w.dayName,exercises:w.exercises,createdAt:new Date().toISOString()}); s.activeWorkout=null; const p=findProgramById(s.selectedProgramId); if(p) s.currentProgramDayIndex=(s.currentProgramDayIndex+1)%p.days.length;} else {s.activeWorkout=w;}} else {w.currentSetIndex += 1; s.activeWorkout=w;} setState(s); renderAll();});

function createExerciseRow(dayIdx, value=""){const options=getAllExercises().map(e=>`<option value="${e.name}" ${e.name===value?"selected":""}>${e.name}</option>`).join(""); return `<div class="exercise-row"><select name="exercise-${dayIdx}">${options}</select><button type="button" class="outline-btn tiny-btn" data-remove-exercise>Ta bort</button></div>`;}
function attachExerciseButtons(scope=document){scope.querySelectorAll("[data-add-exercise]").forEach(btn=>btn.onclick=()=>{const list=btn.closest(".custom-day-card").querySelector("[data-exercise-list]"); list.insertAdjacentHTML("beforeend", createExerciseRow(btn.dataset.dayidx)); attachExerciseButtons(list.parentElement);}); scope.querySelectorAll("[data-remove-exercise]").forEach(btn=>btn.onclick=()=>{const list=btn.closest("[data-exercise-list]"); if(list.querySelectorAll(".exercise-row").length>1) btn.closest(".exercise-row").remove();});}
function renderBuilder(freq, existingDays=null){switchView("programs"); customProgramDays.innerHTML=""; for(let i=0;i<freq*2;i++){const week=Math.floor(i/freq)+1; const pass=(i%freq)+1; const idx=`d${i}`; const day=existingDays?existingDays[i]:null; const rows=(day&&day.exercises&&day.exercises.length?day.exercises:["Bänkpress","Latsdrag","Axelpress","Bicepscurl"]).map(ex=>createExerciseRow(idx, ex)).join(""); const card=document.createElement("div"); card.className="custom-day-card"; card.innerHTML=`<h4>Vecka ${week} – Pass ${pass}</h4><div class="form-grid"><label class="full">Passnamn<input type="text" name="dayName-${idx}" value="${day?day.name.replace(/^Vecka \d+ – /,''):''}" placeholder="Till exempel Bröst + Rygg" /></label><label>Muskelgrupp 1<select name="muscle1-${idx}"><option value="Bröst">Bröst</option><option value="Rygg">Rygg</option><option value="Ben">Ben</option><option value="Axlar">Axlar</option><option value="Armar">Armar</option><option value="Mage">Mage</option></select></label><label>Muskelgrupp 2<select name="muscle2-${idx}"><option value="">Ingen</option><option value="Bröst">Bröst</option><option value="Rygg">Rygg</option><option value="Ben">Ben</option><option value="Axlar">Axlar</option><option value="Armar">Armar</option><option value="Mage">Mage</option></select></label><div class="full"><strong>Övningar</strong><div data-exercise-list>${rows}</div><div class="button-row" style="margin-top:10px;"><button type="button" class="secondary tiny-btn" data-add-exercise data-dayidx="${idx}">Lägg till övning</button></div></div></div>`; customProgramDays.appendChild(card);} customProgramBuilder.classList.remove("hidden"); attachExerciseButtons(customProgramDays);}
function resetEditor(){const s=getState(); s.editingProgramId=null; setState(s); customProgramTitle.textContent="Skapa eget 2-veckorsupplägg"; saveProgramBtn.textContent="Spara eget upplägg"; cancelEditProgramBtn.classList.add("hidden"); customProgramBuilder.classList.add("hidden"); customProgramDays.innerHTML="";}
function startEditProgram(id){const s=getState(); const p=(s.customPrograms||[]).find(x=>x.id===id); if(!p)return; s.editingProgramId=id; setState(s); customProgramTitle.textContent="Redigera eget upplägg"; saveProgramBtn.textContent="Spara ändringar"; cancelEditProgramBtn.classList.remove("hidden"); customProgramName.value=p.name; customProgramFrequency.value=String(p.days.length/2); renderBuilder(Number(customProgramFrequency.value), p.days);}
customProgramSetupForm.addEventListener("submit", e=>{e.preventDefault(); renderBuilder(Number(customProgramFrequency.value));});
cancelEditProgramBtn.addEventListener("click", resetEditor);
customProgramBuilder.addEventListener("submit", e=>{e.preventDefault(); const s=getState(); const freq=Number(customProgramFrequency.value); const name=customProgramName.value.trim()||"Eget upplägg"; const days=[]; for(let i=0;i<freq*2;i++){const week=Math.floor(i/freq)+1; const pass=(i%freq)+1; const idx=`d${i}`; const dayName=e.target.querySelector(`[name="dayName-${idx}"]`).value.trim()||`Pass ${pass}`; const m1=e.target.querySelector(`[name="muscle1-${idx}"]`).value; const m2=e.target.querySelector(`[name="muscle2-${idx}"]`).value; const exercises=[...e.target.querySelectorAll(`[name="exercise-${idx}"]`)].map(el=>el.value).filter(Boolean); days.push({name:`Vecka ${week} – ${dayName}`, muscles:[m1,m2].filter(Boolean), exercises});} let edited=false; if(s.editingProgramId){s.customPrograms=s.customPrograms.map(p=>p.id===s.editingProgramId?{...p,name,description:`Eget tvåveckorsupplägg • ${freq} pass per vecka`,type:"custom",days}:p); s.selectedProgramId=s.editingProgramId; edited=true;} else {const id="custom-"+Date.now(); s.customPrograms.unshift({id,name,description:`Eget tvåveckorsupplägg • ${freq} pass per vecka`,type:"custom",days}); s.selectedProgramId=id;} s.currentProgramDayIndex=0; s.activeWorkout=null; s.editingProgramId=null; setState(s); customProgramMessage.classList.remove("hidden"); customProgramMessage.textContent=edited?"Upplägget uppdaterades.":"Eget upplägg sparades och valdes."; resetEditor(); renderAll(); switchView("programs");});

settingsForm.addEventListener("submit", e=>{e.preventDefault(); const s=getState(); s.settings.userName=userName.value.trim(); s.settings.defaultRound=defaultRound.value; setState(s); settingsMessage.classList.remove("hidden"); settingsMessage.textContent="Inställningarna sparades."; renderAll();});
clearHistory.onclick=()=>{const s=getState(); s.workouts=[]; s.activeWorkout=null; setState(s); renderAll();};
exportData.onclick=()=>{const s=getState(); const blob=new Blob([JSON.stringify(s,null,2)],{type:"application/json"}); const url=URL.createObjectURL(blob); const a=document.createElement("a"); a.href=url; a.download="min-traning-data.json"; a.click(); URL.revokeObjectURL(url);};
function renderAll(){const s=getState(); userName.value=s.settings.userName||""; defaultRound.value=s.settings.defaultRound||"2.5"; renderDashboard(); renderPrograms(); renderLibrary(); renderHistory(); renderWorkout();}
renderAll();
